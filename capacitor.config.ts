import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vue3-by-example-calculator-app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
